alias gits='git status'
alias gita='git add'
alias gitau='git add -u'
alias gitd='git diff'
alias gitdc='git diff --cached'
alias gitc='git commit -v'
alias gitl='git log -p'
alias gitlg='git log -p -g'
alias gitlorig='git log ORIG_HEAD..HEAD --pretty=format:"%Cblue%h%Creset %Cgreen%an%Creset %s"'
alias gitlor='gitlorig | cat && echo'
alias gitlorp='gitlorig -p'
alias gitup='git pull && gitlor && git submodule update'
alias gitp='gitup && git push'

alias git-set-remote='echo git config branch.master.remote "$1" && echo git config branch.master.merge "refs/heads/$2"'
